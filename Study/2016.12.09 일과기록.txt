2016.12.09 금요일

오전
1. 웹 플젝(ColumnMapping)
  - 조회 페이지 첫 진입 시 속도 향상 고민
  
    1) DB의 문제인가?
       -> ResultSet.setFetchSize() 이용하여 fetch를 늘려봄
          ...50 row 정도의 적은 데이터이기에 효과가 없었음
       -> select 쿼리의 속도 성능을 실험해 봄
          ...SQL Developer에서 쿼리 실행속도를 관찰해보고 DB 문제가 아니라는 판단을 함
          
    2) JVM의 문제인가?
       -> JSP의 Servlet 변환속도를 의심해 봄(c:forEach 등...)
          ...그러나 딱히 성능을 크게 향상시킬 수 있는 부분을 발견하지 못함
    
    3) 브라우저의 로딩 문제인가? -> 이 문제라고 결론 내렸으나 딱히 해결법을 찾지 못함
       -> HTML의 속도 향상을 위해 줄 바꿈을 탭으로 변경해봄
          ...미세하게 효과를 보였으나 크게 체감되지 않음
       -> 이외의 HTML 로딩시간 단축법을 서칭하였으나 
          적용할만한 사례를 찾지 못함

오후
1. 에어비앤비 모작 가이드
  - 프로필 수정 페이지 제작
    1) HTML 코딩
    2) CSS 코딩
  	3) 해당 페이지 제작 남은 프로세스:
  	     ...페이지 url 할당(Controller)
  	     ...페이지 데이터 바인딩
  	     ...수정버튼 액션
  	         -> 수정 시 필수입력 사항에 대한 validate 기능(자바스크립트)
  	         -> 수정 액션(DB update)
			 -> 수정 후 return되는 페이지?

2. 포트폴리오 작업
  - Github
    1) repository 생성
    2) 제작한 프로그램 업로드(홈페이지 모작, 폴더변경감지 및 업로드 프로그램/업로드 데이터 조회 프로그램)
    
     